# Waffler — macOS Requirements
# Install with:  pip install -r requirements.txt

# ── Audio ──────────────────────────────────────────────────────────────
sounddevice>=0.4.6
numpy>=1.24.0

# ── OpenAI (transcription + styling) ──────────────────────────────────
openai>=1.0.0

# ── Groq (fast transcription + styling) ──────────────────────────────
groq>=0.4.0

# ── Web UI ────────────────────────────────────────────────────────────
pywebview>=4.0

# ── Clipboard ────────────────────────────────────────────────────────
pyperclip>=1.8.2

# ── Config / utilities ──────────────────────────────────────────────
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0

# ── macOS menu bar ───────────────────────────────────────────────────
rumps>=0.4.0

# ── macOS system frameworks (PyObjC) ────────────────────────────────
pyobjc-core>=9.0
pyobjc-framework-Cocoa>=9.0
pyobjc-framework-Quartz>=9.0
# v3.14.32 — for the proper mic TCC check at startup via
# AVCaptureDevice.authorizationStatusForMediaType_(AVMediaTypeAudio).
# v3.14.31 added AVFoundation to PyInstaller hiddenimports but the
# package wasn't actually installed in the build venv, so the import
# failed at startup and the [mic-tcc] diagnostic line never reached
# the log. Adding the requirement fixes that.
pyobjc-framework-AVFoundation>=9.0
