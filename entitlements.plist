<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Allow event monitoring for Fn key detection -->
	<key>com.apple.security.device.audio-input</key>
	<true/>

	<!-- Required for CGEventTap to work -->
	<key>com.apple.security.automation.apple-events</key>
	<true/>

	<!-- Network access for API calls -->
	<key>com.apple.security.network.client</key>
	<true/>

	<!-- Disable App Sandbox to allow system-wide event monitoring -->
	<key>com.apple.security.app-sandbox</key>
	<false/>

	<!-- Allow Input Monitoring -->
	<key>com.apple.security.device.input-monitoring</key>
	<true/>

	<!-- JIT compilation. Required by numba/llvmlite (pulled in by mlx-whisper
	     for audio preprocessing) and by faster-whisper's ctranslate2 backend.
	     Without these, the hardened runtime sends SIGKILL with
	     "Code Signature Invalid / Invalid Page" the first time the library
	     tries to map an executable page. -->
	<key>com.apple.security.cs.allow-jit</key>
	<true/>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
</dict>
</plist>
